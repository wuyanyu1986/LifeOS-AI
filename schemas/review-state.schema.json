{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lifeos.local/schemas/review-state.schema.json",
  "title": "LifeOS Diary Review State",
  "type": "object",
  "required": [
    "entry_date",
    "parsed_note",
    "video_script",
    "wechat_article",
    "pipeline_status"
  ],
  "properties": {
    "entry_date": {
      "type": "string",
      "format": "date"
    },
    "pipeline_status": {
      "enum": [
        "parsed_pending_review",
        "parsed_changes_requested",
        "generating_derivatives",
        "derivatives_pending_review",
        "preparing_wechat_draft",
        "wechat_draft_login_required",
        "wechat_draft_interaction_required",
        "wechat_draft_save_confirmation_required",
        "wechat_draft_save_verification_required",
        "wechat_draft_pending_review",
        "ready_for_manual_publish",
        "completed",
        "failed"
      ]
    },
    "parsed_note": {
      "$ref": "#/$defs/reviewableOutput"
    },
    "video_script": {
      "$ref": "#/$defs/reviewableOutput"
    },
    "wechat_article": {
      "$ref": "#/$defs/reviewableOutput"
    },
    "wechat_mp_draft": {
      "$ref": "#/$defs/wechatMpDraft"
    }
  },
  "$defs": {
    "wechatMpDraft": {
      "type": "object",
      "required": ["status", "revision", "notification_status"],
      "properties": {
        "status": {
          "enum": [
            "not_created",
            "preparing",
            "login_required",
            "interaction_required",
            "editing",
            "save_confirmation_required",
            "save_verification_required",
            "pending_review",
            "ready_for_manual_publish",
            "failed"
          ]
        },
        "revision": {
          "type": "integer",
          "minimum": 0
        },
        "cover_path": {
          "type": ["string", "null"]
        },
        "cover_square_path": {
          "type": ["string", "null"]
        },
        "browser_package_path": {
          "type": ["string", "null"]
        },
        "browser_package_fingerprint": {
          "type": ["string", "null"]
        },
        "editor_url": {
          "type": ["string", "null"]
        },
        "cover_media_id": {
          "type": ["string", "null"]
        },
        "draft_media_id": {
          "type": ["string", "null"]
        },
        "created_at": {
          "type": ["string", "null"],
          "format": "date-time"
        },
        "notification_status": {
          "enum": ["not_sent", "sent", "failed"]
        },
        "notification_message_id": {
          "type": ["string", "null"]
        },
        "last_error": {
          "type": ["string", "null"]
        }
      },
      "additionalProperties": false
    },
    "reviewableOutput": {
      "type": "object",
      "required": ["status", "revision", "notification_status"],
      "properties": {
        "wiki_node_token": {
          "type": ["string", "null"]
        },
        "doc_token": {
          "type": ["string", "null"]
        },
        "status": {
          "enum": [
            "not_created",
            "pending_review",
            "approved",
            "changes_requested",
            "superseded"
          ]
        },
        "revision": {
          "type": "integer",
          "minimum": 0
        },
        "notification_status": {
          "enum": ["not_sent", "sent", "failed"]
        },
        "notification_message_id": {
          "type": ["string", "null"]
        },
        "reviewer_open_id": {
          "type": ["string", "null"]
        },
        "reviewed_at": {
          "type": ["string", "null"],
          "format": "date-time"
        },
        "review_comment": {
          "type": ["string", "null"]
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
